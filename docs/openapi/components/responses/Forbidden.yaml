description: Forbidden to perform an action
content:
  application/json:
    schema:
      title: Forbidden
      type: object
      additionalProperties: false
      properties:
        type:
          type: string
        title:
          type: string
        detail:
          type: string
      required:
        - type
        - title
        - detail
